name: Apply community priority patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-community-priority-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python scripts/patch_community_priority.py
      - name: Validate syntax
        run: python -m py_compile app.py
      - name: Commit app update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          git diff --cached --quiet || git commit -m "feat: surface Latino-owned and STEM community prospects"
          git push
