# Data notes

The seed dataset uses real organizations and public first-party sources.

Important:
- Coordinates are approximate facility/HQ coordinates used only for the radius demo.
- A source URL is retained for each evidence claim.
- A missing signal is **not** treated as proof that a program does not exist; it means the prototype has not verified it yet.
- Scoring is deterministic from verified evidence flags plus transparent organization metadata.
